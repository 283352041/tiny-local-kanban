# Tiny Local Kanban

A single-file local-first kanban board. It stores tasks in `localStorage` and exports the board as Markdown.

Open `index.html` in a browser. Click a card to move it to the next column.
